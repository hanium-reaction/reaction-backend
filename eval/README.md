# `eval/` — 오프라인 평가 데이터셋

참가자 없이(사람 0명) 돌리는 L1 오프라인 평가의 입력이 여기 산다.
설계·지표 정의는 [`docs/experiments/experiment-plan-v1.md`](../docs/experiments/experiment-plan-v1.md) 가 단일 진실 소스다.

## `golden_recovery_cases.jsonl` — 회복 골든셋 120건

| 블록 | 건수 | 무엇을 보는가 |
|---|---|---|
| `single_tag` | 52 (13태그 × 4) | 태그당 짧은/긴 카드 × 오전/야간 블록 |
| `multi_tag` | 26 (13 × 2) | 함께 선택될 만한 2태그 조합에서 전략이 경합하는지 |
| `uncovered_tag` | 12 (3 × 4) | `TIME_SHORTAGE`/`OVERRUN`/`AVOIDANCE` — ⚠️ 블록 이름은 역사적(PR #256 당시 미커버). 지금은 #257 신설 4전략으로 실매칭됨(`tests/test_recovery_catalog_sync.py::test_all_thirteen_tags_are_now_covered`) — 이 블록은 그 회귀 확인용 |
| `boundary` | 20 | overwhelm 3/4/5, 연속실패 2/3/5, 23시 근접, 이력 0, 태그 미선택, 계약 위반(3태그) 등 |
| `adversarial` | 10 | 자기비난 회고 — 시스템이 그 프레임을 되받아 쓰는지 |

### 생성·재현

```bash
uv run python -m scripts.build_golden_recovery_cases
```

난수도 현재시각도 쓰지 않는다. 같은 커밋은 항상 같은 파일을 만들고,
`tests/test_golden_recovery_cases.py::test_file_on_disk_matches_the_generator` 가
**커밋된 파일 == 생성기 출력**을 고정한다. 생성기를 고쳤으면 파일도 다시 생성해 커밋할 것.

### 읽을 때 주의할 것

- **전 케이스 `synthetic: true`.** 라이브 `recovery_attempts` 가 0건이라(2026-08-17 실측)
  실 로그를 한 건도 쓰지 못했다. 보고서에 합성 비율을 반드시 명시한다.
- **`design_intent` 는 정답이 아니다.** 설계자가 쓴 것이므로 룰 엔진의 "정확도"로 쓰면
  자기충족적이다. 패딩률·커버리지 같은 **구조적 지표**와 사람 라벨링의 출발점으로만 쓴다.
- **`assertions.must_not_contain` 은 케이스 고유 추가분뿐이다.** 전역 금지어는 저장하지
  않고 `safety/banned_words.BANNED_REPLACEMENTS` 에서 읽는다 — 사전이 두 곳으로 갈라지면
  한쪽만 고쳐지기 때문이다.
- 적대적 케이스의 회고 문구에는 금지어(`실패했`, `한심` 등)가 **일부러 들어 있다.**
  금지어 필터는 **시스템 출력**에만 걸린다 — 사용자가 쓴 말을 검열하는 장치가 아니다.

## `golden_materials_cases.jsonl` — 자료 골든셋 48건

계획 분해가 **주어진 자료를 얼마나 충실히 따르는가**를 재는 오프라인 입력(L1-6).
회복 골든셋이 회복 문구만 덮고 있어 비어 있던 축이다.

| 블록 | 건수 | 무엇을 보는가 |
|---|---|---|
| `grounded_clean` | 12 (6자료 × 마감 2종) | 목차가 계획의 뼈대가 되는가 (M13 적중률) |
| `grounded_noisy` | 6 | 네비게이션·푸터 잡음이 섞여도 목차만 골라 쓰는가 (M15) |
| `omission_probe` | 12 | 자료에 **일부러 없는** 흔한 주제를 끌어오는가 (M14) |
| `no_material` | 6 | 자료 없음 — **M14 의 비교 기준선**. 여기선 그 주제가 나오는 게 정상이다 |
| `unfetchable` | 4 | 링크만 주고 못 여는 경우 — 되묻기 기대, 내용 지어내기 금지 (M16, #226) |
| `adversarial_injection` | 8 | 자료에 심긴 지시문을 따르는가 (#260 회귀) |

### 생성·재현

```bash
uv run python -m scripts.build_golden_materials_cases
```

회복 골든셋과 같은 규칙 — 난수도 현재시각도 쓰지 않고,
`tests/test_golden_materials_cases.py::test_file_on_disk_matches_the_generator` 가
**커밋된 파일 == 생성기 출력**을 고정한다.

### 읽을 때 주의할 것

- **자료 본문이 전부 합성이다.** 실재하는 교재·강의 목차를 쓰지 않은 이유는 세 가지다 —
  ① 저작권(#259 §4.1 ④ 가 "목차·커리큘럼까지만" 으로 그은 경계) ② 외부 URL 본문을 정답으로
  쓰면 페이지가 바뀌는 순간 골든셋이 조용히 썩는다 ③ **할루시네이션을 재려면 모델이 모르는
  자료여야 한다** — 2026-08-22 실측에서 파이썬 공식 튜토리얼(모델이 아는 자료)로는 자료 없는
  대조군이 이미 9/9 를 맞혀 자료의 기여가 보이지 않았다.
- **`anchor_items` 가 1차 지표다.** 각 자료에는 목표 제목·도메인 상식으로는 나올 수 없는
  고유 문자열('오르카', 'F-13', '한터 사전' 등)이 심겨 있다. 계획에 등장하면 자료를 실제로
  읽었다는 증거이고, `no_material`/`unfetchable` 에서 등장하면 **아는 척**이다. 1차 실행
  (2026-08-22)에서 `expected_items` 로 아는 척을 판정했다가 실패한 걸 대체한다 — '가설검정'
  같은 항목은 목표가 "통계학 입문서 완주" 면 자료 없이도 나와 증거가 못 됐다.
- **1차 실행에서 상식 유입이 확인된 금지어는 뺐다** — `회귀분석`·`베이즈`(통계) · `포크`(OSS).
  자료 없이도 나오는 주제를 금지어로 두면 M14 가 상식 유입을 할루시네이션으로 오계수한다.
- **`forbidden_items` 등장률(M14)을 그대로 할루시네이션율로 쓰면 안 된다.** 자료가 없을 때
  그 주제가 나오는 건 정상이므로 `no_material` 대비 증감으로만 읽는다. 그래서 두 블록은
  **같은 금지어 집합**을 쓰도록 테스트가 고정한다.
- **`forbidden_items` 와 `noise_items` 는 정반대 불변식이다.** 전자는 자료에 **없어야** 하고
  (없는 걸 지어냈는지), 후자는 자료에 **있어야** 한다(있는 잡음을 그대로 베꼈는지). 한
  필드로 합치면 두 원인이 한 수치로 섞인다.
- **적대 블록의 통과는 "방어가 동작했다" 가 아니다.** 2026-08-22 라이브 실측에서 이 계열
  공격은 #260 가드를 제거해도 관철되지 않았다(before/after 모두 0/8). 양성 대조군이 없으므로
  이 블록은 **회귀 감시**로만 쓴다.
- 마감은 `deadline_offset_days`(상대값)뿐이다 — 절대 날짜를 넣으면 하루만 지나도 판정이
  뒤집힌다(`s10_corners.py` 전례).

## `golden_first_plan_cases.jsonl` — 첫 계획 골든셋 84건

첫 계획이 **사용자가 인터뷰에서 말한 제약을 지키는가**(L1-7A)와 **LLM 검토기(④층)가 제
역할을 하는가**(L1-7B)를 재는 오프라인 입력. 결함의 정의는
[`rubric-first-plan-v1.md`](../docs/experiments/rubric-first-plan-v1.md) 가 단일 진실 소스다.

| 블록 | `kind` | 건수 | 무엇을 보는가 |
|---|---|---|---|
| `normal` | decompose | 12 | 6목표 × 마감 2종(28일/70일) — L1-7A 기준선 |
| `constraint_edge` | decompose | 12 | 집중 용량 ±5분 격자 — 15/50/90/120 각각 −5·0·+5. **반려율 곡선의 x 축** |
| `milestone_fixed` | decompose | 6 | 외부에서 날짜가 고정된 목표 + **확정 마일스톤 3~4개** — 지평 커버리지·배치, 그리고 **M23(마일스톤 충실도)·M24(범위 이탈)의 유일한 입력** |
| `busy_saturated` | decompose | 4 | 요구량이 가용량에 붙거나 넘는 포화 — 분량 절단 |
| `defect_free_control` | verify | **30** | **M29 `false_reject_rate` 의 분모.** 여기서 나온 반려는 전부 오탐. 12→30 으로 늘렸다 — 0/12 의 단측 95% 이항 상한이 **0.221** 이라 사전등록 임계값 ≤0.10 을 확인할 수 없었다 |
| `seeded_defect` | verify | 20 | 2기준계획 × D1~D5 × easy/boundary |

### 한 파일에 두 종류가 산다 (`kind`)

- **`decompose`** — 인터뷰 슬롯만 담는다. 하네스가 ②③층을 태워 계획을 **만든다**.
- **`verify`** — **완성된 계획을 담는다.** 하네스는 그걸 ④층에만 먹인다. 검토기를 재려면
  입력이 고정돼야 한다 — 매번 새로 분해하면 검토기 성능과 분해기 변동이 한 수치에 섞인다.

`verify` 블록의 계획은 손으로 쓴 원안을 **프로덕션과 같은 ③층 보정 체인**에 태운 결과다.
검토기가 운영에서 보는 것이 ③층 출력이기 때문이다 — 손으로 쓴 계획을 먹이면 `N회차`
채움이나 클램프된 길이처럼 **오탐이 가장 잘 나는 산물**이 골든셋에 아예 없게 된다.

### 결함은 다른 모델이 설계했다 — `first_plan_seeded_defects.json`

체크리스트를 쓴 쪽이 심을 결함까지 고르면 "자기가 만든 버그만 잡는 린터"를 측정하게 된다
(계획서 L1-7B 순환성 항목). 그래서 결함 **내용**은 상위 세션과 **다른 모델**이 D1~D5 의
한 줄 정의와 기준 계획 JSON 만 보고 작성했고, 루브릭 §2 앵커 표와 §3 주입 레시피는
가렸다. 무엇을 보여주고 무엇을 가렸는지는 그 파일의 `provenance` 에 남아 있다.

이 생성기는 그 파일의 **연산(op)을 결정적으로 적용**하기만 한다. 손으로 고치지 말 것 —
고치면 held-out 조건이 깨지고 `provenance` 가 거짓이 된다.

⚠️ **완화이지 해소가 아니다.** 결함 **분류 체계**(D1~D5) 자체는 여전히 루브릭 작성자의
것이다. 분류 밖의 실패 유형은 이 골든셋으로 영원히 안 잡힌다. 실제로 held-out 설계자가
분류 체계의 빈칸 3개를 보고했고, 그건 루브릭 §6 에 기록돼 있다.

### 생성·재현

```bash
uv run python -m scripts.build_golden_first_plan_cases
uv run python -m scripts.build_golden_first_plan_cases --dump-base-plans  # held-out 의뢰용
```

앞의 두 골든셋과 같은 규칙 — 난수도 현재시각도 쓰지 않고,
`tests/test_golden_first_plan_cases.py::test_file_on_disk_matches_the_generator` 가
**커밋된 파일 == 생성기 출력**을 고정한다.

### 읽을 때 주의할 것

- **전 케이스 `synthetic: true`.** 실사용 인터뷰 슬롯 분포를 대표하지 않는다. 특히
  `session_length` 미답 경로가 실사용에서 얼마나 흔한지 모른다.
- **`boundary` 케이스의 정답은 '통과'다.** '조금 덜 심한 결함'이 아니라 **결함으로 오해받기
  쉬운 정상**이다(`expected.approved == true`). 120분 사고가 정확히 이 형태였다 — 사용자
  상한에 정확히 붙은 **정당한** 값을 검토기가 3/3 반려했다. easy 만으로는 그 실패를 못 잰다.
- **`N회차` 세션은 룰이 붙인 정상 산물이다.** `extend_action_plan_to_horizon` 이 마감까지
  채우는 것이라 제목이 거의 같다. 중복(D1)으로 세면 오탐이다.
- ⚠️ **`N회차` 로 필터링하지 말 것.** `control-cert-standard` 의 사용자 작업 제목
  "SQL 활용 기출 **3회차** 풀기" 가 룰의 채움 세션 명명과 문자열이 겹친다. 정규식으로
  회차 세션을 걸러내면 **실제 사용자 작업이 같이 날아간다.** 채움 여부는 제목이 아니라
  `node_id` 접두사(`tmp-continue-`)로 판정한다.
- **채움 세션 비율이 케이스마다 크게 다르다** (0% ~ 85%). 원안이 짧을수록 룰이 많이 채운다.
  D1 오탐률을 블록 평균으로만 보면 이 비율에 끌려다닌다 — 케이스별로 같이 보고한다.
- **M29 의 표본을 30건으로 올렸다(2026-09-02).** 12건일 때는 해상도가 1/12 ≈ 0.083 이라
  임계값을 겨우 분해했고, 더 나쁘게는 **0건 반려여도 단측 95% 상한이 0.221** 이라
  "≤0.10 을 확인했다" 고 말할 수 없었다. 30건이면 0건일 때 상한이 **0.095** 다
  (`1 − 0.05^(1/n)`). ⚠️ rule of three(`3/n = 0.100`)는 근사이고 임계값과 같은 값이라
  "겨우 걸친다" 로 오독된다 — 문서에는 정확값을 쓴다.
  ⚠️ **반복 3회를 90건으로 세면 안 된다** — 같은 케이스의 반복은 상관이 있어 독립 표본이
  아니다. 분모는 케이스 수다. 그리고 30건은 **0건 반려일 때만** 성립하는 하한이라, 1건이라도
  반려되면(1/30) 상한이 다시 0.10 을 넘는다.
- 마감은 `deadline_offset_days`(상대값)뿐이다 — 절대 날짜를 넣으면 하루만 지나도 판정이
  뒤집힌다(`s10_corners.py` 전례). 저장된 `verify` 계획은 같은 오프셋으로 다시 구우면
  재현된다.
- ⚠️ **held-out 주장을 D4·D5 에서는 약하게 읽을 것.** 루브릭 앵커 표의 예시 목표와 기준
  계획의 목표가 겹쳐서, 그 두 유형은 실제 결함 문구가 앵커 어휘와 수렴했다. 유출인지
  우연인지 구별할 수 없다 — 자세한 것은 `first_plan_seeded_defects.json` 의
  `provenance.audit` 과 루브릭 §6.
- ⚠️ **D2 easy 는 `first_step` 이 그대로라** 항목 전체로는 착수 가능할 수 있다(연산이
  케이스당 1개라는 제약 때문). 그 유형의 통과를 곧바로 "검토기가 못 잡았다"로 읽지 말 것.

### 아직 안 고친 것 — 보고서가 인용할 한계 (2026-09-02 독립 감사 2차)

> 아래는 **고칠 수 있지만 아직 안 고친 것**이다. 이 목록 자체가 보고서의 「한계」 절
> 근거이므로, 서술이 부정확하면 그 부정확함이 그대로 인용된다. 각 항목은 감사 결과를
> 실측으로 재확인한 뒤 적었다.

| # | 무엇 | 어디까지 참인가 |
|---|---|---|
| **1** | **마일스톤은 `milestone_fixed` 6건에만 있다 — M26 은 아직 계산 불가** | 2026-09-02 에 `milestone_fixed` 6건이 확정 마일스톤 19개와 `milestone_cursor` 를 싣게 됐다. 프로덕션 함수로 확인한 실제 값(`horizon_weeks` 는 `_MAX_PLAN_WEEKS`=4, `contest` 만 마감이 21일이라 3): **M23 의 분모가 그 6건에서 2~4 로 0 이 아니게 됐고**, `drop_out_of_cycle_branches` 가 `exam` 1 · `conference` 2 · `visa` 1 · `recital` 1 개 branch 를 실제로 걷어내 **M24 를 잰다**(`contest`·`defense` 는 마감이 계획 지평 안이라 창이 전부를 덮는 게 정상이고 0 이다). ⚠️ **이 행의 아래 서술은 2026-09-02 시점의 것이고, 그 뒤 해소됐다** — 2026-09-03 에 하네스 `scripts/l1_7_run.py` 가 작성되고 배치 경로(#418)가 배선되면서 **M26-core 가 실제로 산출됐다(0.794 = 27/34, [`l1-7-results.md`](../docs/experiments/l1-7-results.md) §6.2).** 당시 막고 있던 셋 중 ①은 N/A 를 실패도 제외도 아닌 **중립**으로 처리해, ②는 M18 을 AND 에서 빼고 분포로 따로 보고해, ③은 하네스를 써서 각각 풀렸다. ⚠️ **다만 M26-core 를 어느 지점에서 재는지는 여전히 문서마다 다르다** — [#464](https://github.com/hanium-reaction/reaction-backend/issues/464). *(원 서술 보존: "① `normal`·`constraint_edge`·`busy_saturated` 28건에는 마일스톤이 없어 M23 분모가 0 이다. ② M18·M19·M24·M25 는 비율인데 통과 임계값을 §191 이 사전 고정하지 않기로 했다. ③ 하네스가 없어 아무것도 계산되지 않는다.")* |
| 2 | **M27·M28 이 두 문서에서 다르게 정의됨** | M27: 실험계획서 = `approved=false` / 루브릭 = `findings` 에 severity ≥ 2. M28: 실험계획서 = **유형** 지목 / 루브릭 = **노드** 지목 — 아예 다른 지표가 같은 ID 를 쓴다. 두 문서 **모두** 자기가 단일 진실 소스라고 선언하고 있다 |
| 3 | **held-out 오염이 D4·D5 를 넘는다** | 결함 작성자에게 **보여준** op 어휘 4종이 감춘 §3 레시피의 D2·D3·D4 행을 그대로 재현하고, D5 의 `category` 페이로드도 마찬가지다. 다섯 유형 전부에 닿는다. 매핑은 **5종 → 4종**이다(`insert_item` 이 D1·D5 겸용) — 1:1 이 아니다. `provenance.withheld` 의 "레시피는 안 넘겼다"는 서술이 부분적으로 거짓 |
| ~~4~~ | ~~`d5-easy` 2건의 `category: "life"`~~ | ✅ **2026-09-02 해소.** 두 건을 재의뢰해 교체했다(`provenance.retired`). 이제 `category` 는 어느 쌍에서도 easy/boundary 를 가르지 않는다. ⚠️ 다만 당시 서술 하나를 정정해 둔다 — 감사는 `"life"` 를 "프로덕션이 낼 수 없는 값" 이라 했으나 **아니다**: `_normalize_category` 는 **영속화(approve) 시점**에 돌고 검토기(④층)는 그보다 앞에서 raw 값을 본다(`_review_variables` 가 `a.model_dump()` 를 통째로 넘긴다). 프롬프트가 어휘를 제약할 뿐이라 LLM 이 어기면 검토기까지 간다 — 정확히는 **off-spec 이지만 도달 가능**이었다. 실제 문제는 어휘 위반이 아니라 지름길이었다 |
| ~~5~~ | ~~`d1-easy-cert` 가 루브릭 D1 정상 앵커와 모순~~ | ✅ **2026-09-02 해소.** 재의뢰해 교체했다. ⚠️ **이 행의 서술은 낡았다** — 여기 적힌 "3회차 **다시** 풀기" 는 3차 재의뢰(`4a57402`)가 다시 "SQL 활용 3회차 기출문제 풀이하기" 로 교체했다(`다시` 가 D1 의 완전 분리자가 돼서). 당시 논리는 이랬다: 새 결함은 루브릭 D1 **결함 행 1**("제목이 표기만 다르고 대상·행위가 같다")에 정확히 걸린다. 정상 행 2 의 "회독·복습·심화" 예시는 "3강 시청 / 3강 예제 직접 풀기" 처럼 **행위가 다른** 경우인데, 새 것은 행위가 동일하고 부사만 붙었다. boundary 가 **4회차**(다른 대상)라 축도 선명해졌다. 옛 항목은 `provenance.retired.entries` 에 보존 |
| **4′** | **D4 `first_step` 의 길이·숫자는 해소, 영문은 미해소** | 길이는 3차 재의뢰로 겹치게 됐다 — 다만 **바뀐 쪽은 boundary 다**: easy 는 `[10,14]` 그대로고 boundary 가 `[26,29]` → `[10,13]` 으로 깎였다(`d4-easy-*` 는 재의뢰 대상 9건에 없었다). 앞서 이 자리에 "easy `[10,14]`→`[9,12]`" 라고 적었는데 화살표가 반대쪽에 붙었고 `[9,12]` 는 어디에도 없는 숫자다. 숫자 신호도 사라졌다. 그러나 이 행이 함께 지목한 **"영단어 포함"** 은 남아 있다 — `d4-boundary-cert` 의 `SQL 기출문제집 펼치기`. 검사기에 `first_step_has_latin` 이 없어서 안 보였을 뿐이다. 「M27·M28 을 읽을 때」 참조 |
| **5′** | **`d1-*-portfolio` 의 easy/boundary 차이는 해소, 형제 대비 이탈은 남음** | 두 op 가 모두 `after: l2`·60분이 되어 **서로 다르다**는 tell 은 사라졌다. 그러나 이 행이 함께 적은 "형제 카드가 전부 120분인데 거기서 벗어난다" 는 그대로다 — 오히려 양쪽 다 이탈해 **M28b 정답 키가 `argmin(분량)` 으로 균일하게 노출**된다 |
| **9** | **기계적 게이트가 내용 품질을 깎았다 (이번 접근의 비용)** | 지름길 14건을 없애려고 결함 20건 중 **9건**(D1×3·D2×2·D4×2·D5×2)을 재의뢰했는데, 통과 판정을 **사람이 아니라 `scripts/check_seeded_defect_shortcuts.py`** 가 하게 한 결과 세 곳에서 내용이 나빠졌다. ① D1 boundary-portfolio 는 앵커 일치 규칙 때문에 **시나리오가 통째로 바뀌었다** — 원래(배포 후 교차 브라우저 확인)가 더 선명한 경계였다. ② D2 boundary-cert 가 영문·숫자 신호를 피하려 "SQL 활용 단원"→"쿼리 단원" 이 되어 **계획 자신의 어휘와 어긋난다**. ③ D4 boundary 2건이 길이·숫자를 줄이려 "문제집 **1페이지** 펼치기"→"문제집 펼치기" 로 깎여 **"통과가 정답" 이라는 성질이 조금 약해졌다.** 셋 다 작성자가 스스로 보고했고(숨기지 말라고 지시했다), 숨겨진 왜곡보다 기록된 왜곡이 낫다고 판단해 채택했다. 자세한 것은 `first_plan_seeded_defects.json` 의 `provenance.reauthored_2026_09_02` |
| **10** | **D1 에 대해서는 채점 기준 힌트를 의도적으로 흘렸다** | 재의뢰 시 "다시/재-/복습/회독/한 번 더 를 쓰지 말라" 는 제약을 줬다. 감춘 루브릭의 D1 정상 앵커가 회독·복습을 중복이 아니라고 규정하므로 **이것은 힌트가 맞다.** 이 힌트 없이는 같은 자리에 세 번째로 빠질 것이 확실해 전달했다. **held-out 이 순수하다고 주장하지 않는다** — M27a·M27b 의 D1 은 이 단서를 달아 보고한다 |
| ~~6~~ | ~~**`scripts/l1_7_run.py` 가 아직 없다**~~ | ✅ **2026-09-03 해소.** 하네스가 작성돼 **L1-7A**(34케이스 × 3반복 = 102 호출)가 이것으로 돌았다. ⚠️ **L1-7B 는 이 하네스가 아니다** — `scripts/l1_7b_run.py`(v3) · `l1_7b_v4_run.py`(v4) 가 따로 있고, `l1_7_run` 을 임포트하지 않고 `build_outcome` 복원 로직을 복제해 뒀다. 이 모듈을 실제로 재사용하는 것은 **M33 하네스**(`scripts/l1_7b_m33_run.py`)뿐이고, 거기서 `m26_core`·`core_verdicts`·`score_raw` 를 그대로 쓴다(그래서 M33 의 arm 별 M26-core 가 L1-7A 와 같은 함수로 계산된다 — 다만 **입력이 다르다**, [#464](https://github.com/hanium-reaction/reaction-backend/issues/464)) |
| 7 | **§1.1 표의 두 행이 서로 다른 단위** | 1번 행("상한 초과 0")은 **계획 단위** 카운트고, 2번 행(90)은 **슬롯 조합 단위** 카운트다. 계획 단위로 세면 2번 행은 86 이 된다. 행의 산문("전부 `planned_session_min_for < 15` 인 조합")이 슬롯 단위 읽기를 지지하므로 90 을 핀했지만, 한 열 아래 두 종류가 섞여 있다 |
| 8 | **`insert_item` 계획의 채움 회차 번호** | 삽입 후 실제 카드는 5장인데 채움 이름은 "5회차" 부터 시작한다. 새 초안에서 ③층을 처음부터 돌리면 "6회차" 가 됐을 것이다 — 즉 이 계획들은 ③층의 **고정점이지만 새 초안에서 도달 가능한 형태는 아니다**. 수정 전에도 같았다 |

### M27·M28 을 읽을 때 — 남아 있는 지름길 (2026-09-02 확정)

> **이 절이 보고서 「한계」의 핵심이다.** M27a·M27b(결함 탐지)·M28a·M28b(유형/위치 지목)를 유형별로
> 실을 때 아래 단서를 **반드시 함께** 싣는다.

`scripts/check_seeded_defect_shortcuts.py` 를 돌리면 현재 목록이 그대로 나온다. 이 스크립트는
**0 을 목표로 하지 않는다** — 아는 것을 사유와 함께 등록해 두고 **새로 생기는 것만** 막는
래칫이다. `tests/test_golden_first_plan_cases.py` 의 두 테스트가 그 기준선을 지킨다.

| 유형 | 남은 지름길 | M27 을 어떻게 읽어야 하나 |
|---|---|---|
| **D2** | **`마저`** 가 easy 두 계획에만. 무결함 대조군 **0/30** (채움 카드를 넣고 세도 0) | **가장 나쁘다.** "계획에 `마저` 가 있으면 D2 결함" 이라는 한 줄 규칙이 오탐 없이 2/2 를 맞힌다. **M27(D2)를 검토기의 의미 판단으로 읽으면 안 된다** |
| D1 | 심은 `first_step` 의 영문(`README`, portfolio 쌍) + 타깃 위치 | 완전 분리자지만 **심은 문구 안에서만** 갈린다 — `README` 는 두 계획의 기존 카드에도 있어서, 검토기가 M28 을 먼저 풀지 않으면 못 쓴다 |
| D4 | 심은 `first_step` 의 영문(`SQL`) + 타깃 위치 | 〃 |
| D3 | 타깃 위치만 | **가장 깨끗하다** — 어휘·문구 분리자 없음 |
| D5 | 없음 | 3차 재의뢰가 `category` 를 없앤 뒤로 남은 게 없다 |
| 전 유형 | 타깃 노드의 **목록 위치**가 D2·D3·D4 에서 갈린다 | `after_anchor` 가 `insert_item` 에만 걸려 `replace_*` 8건이 위치 검사 밖이었다 |

> ⚠️ **2026-09-02 정정.** 이 표는 원래 D4 의 `하기` 접미와 D5 의 `사이트` 도 "대조군 0건인
> 완전 규칙" 으로 실었는데 **둘 다 제 검사기의 범위 오류가 만든 가짜였다.** 검사기가 채움
> 카드(`tmp-continue-*`)를 빼고 세는데, `_review_variables` 는 검토기에게 계획을 **통째로**
> 넘긴다. 채움 제목이 `개발 포트폴리오 **사이트** 완성 N회차` 이고 채움 `first_step` 이 전부
> `…시작**하기**` 라, 실제 범위로 다시 세면 `사이트` 는 대조군 **2/30**·심은결함 11/20,
> `하기` 는 대조군 **30/30** 에서 발화한다 — 분리자가 아니다. 검사기를 고쳐 기준선이
> 11건에서 **9건**으로 줄었다. **진짜는 `마저` 하나뿐이다.**
>
> ⚠️ **2026-09-03 분모 갱신.** 위 세 숫자는 대조군이 12건이던 때 센 것이라 낡아 있었다.
> **대조군 30건으로 다시 세도 결론은 같다** — `마저` 0/30(여전히 완전 분리자), `사이트`
> 2/30, `하기` 30/30. `마저` 는 분모가 커져 **오히려 더 강한 분리자**가 됐다.

**M28 은 별도로 더 약하다.** `insert_item` 8건 중 **6건**에서 심은 카드가 계획의 유일한
비최빈 분량이라 `argmin(분량)` 하나로 정답 키가 나온다(`d1-*-portfolio`, `d5-*` 4건).
M28 은 20건 전체에 걸리는 지표다 — D2·D3·D4 12건은 분량이 균일해 누출이 없으므로, "검토기가 결함 위치를 짚었다" 로 읽을 수 있는 것은 **20건 중 14건**이다.

#### 왜 네 번째 재의뢰를 하지 않았나

세 번 재의뢰하며 확인한 것: **검사기가 세는 특징만 없애면 최적화가 안 세는 특징으로
흘러간다.**

```
1차: category 가 D5 를 100% 갈랐다        → 없앴다
2차: 그 자리에 '다시' 가 생겼다 (D1)       → 없앴다
3차: '마저'(D2) · '사이트'(D5) · SQL(D4) 이 남았다
```

3차의 `SQL` 이 특히 분명하다 — 작성자가 `d4-boundary-cert` 의 같은 문구에서 **숫자는
지우고 영문은 남겼다.** 검사기가 `first_step` 의 숫자만 세고 영문은 안 셌기 때문이다.

특징을 아무리 늘려도 안 재는 것은 남고, **표본이 유형당 2 대 2 라 연속형 신호는 우연히도
약 1/3 확률로 갈린다.** 즉 "분리자 0" 은 도달 가능한 상태가 아니라 표본 크기의 문제다.
그래서 네 번째 재의뢰 대신 **남은 것을 세어 보고하는 쪽**을 택했다.

#### 재의뢰 자체가 유출 경로였다는 것도 함께 기록한다

2차에서 루브릭 작성자(opus)가 제출본을 눈으로 반려하고 다시 받았는데, 그 **반려 사유가
감춘 앵커의 어휘 그대로**였다("'무관' 이 아니라 '간접 기여'" ↔ 루브릭 D5 경계 행
"기여가 간접적"). 감춘 기준에 대고 제출본을 거르는 것(rejection sampling)은 그 기준의
정보를 산출물로 흘려보낸다. 3차부터 판정을 스크립트로 옮긴 이유다.

## 관련 테스트

| 파일 | 무엇을 고정하나 |
|---|---|
| `tests/test_golden_recovery_cases.py` | 골든셋 무결성 — 블록별 건수, 재현성, 적대적 케이스가 실제로 적대적인지 |
| `tests/test_golden_materials_cases.py` | 자료 골든셋 무결성 — 블록별 건수, 재현성, 정답이 자료 안에 실재하는지, 금지 항목이 자료에 없는지, 적대 케이스가 실제로 적대적인지 |
| `tests/test_recovery_selection_coverage.py` | 룰 엔진 도달 공간 **전수 열거**(92개 입력) — PARK 도달(25/92, GOAL_RECHECK 경유), 카드 수 2~4장, 패딩률(28/203) + 뮤테이션 가드 |
| `tests/test_recovery_catalog_sync.py` | 시드 ↔ conftest 미러 ↔ 설계 3자 동기화 |
| `tests/test_golden_first_plan_cases.py` | 첫 계획 골든셋 무결성 — 블록별 건수, 재현성, 결함 격자 완전성, **뮤테이션 가드**(주입이 no-op 이면 20건 전부 잡힌다), 주입이 ③층 불변식을 깨지 않는지, 대조군의 회귀 4종이 태그가 아니라 **계획에서 실제로 참인지**, held-out 출처 기록 |
| `tests/test_first_plan_verifier_invariants.py` | ④층이 검사해선 안 되는 것 — 보정 후 상한 초과 0/1,620, `focus_capacity` == 클램프 상한, 15분 미만은 의도된 값 |
