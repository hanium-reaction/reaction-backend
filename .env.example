# reaction-backend 환경변수 예시 (.env로 복사 후 사용)

# 실행 환경: local | dev | staging | prod
APP_ENV=local

# 앱 메타
APP_NAME=reaction-backend
APP_VERSION=0.1.0

# 서버 바인딩
HOST=0.0.0.0
PORT=8000

# CORS 허용 origin (JSON 배열로 입력)
CORS_ALLOW_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# ───── 후속 이슈에서 채워질 자리 ─────

# Issue #2 — DB
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/reaction
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Issue #5 — LLM (Gemini)
# GEMINI_API_KEY=
# LLM_MODEL=gemini-2.0-flash-exp
# LLM_TIMEOUT_SECONDS=8
# LLM_MAX_RETRIES=3

# Issue #6 — Google OAuth / Calendar
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=

# Auth
# JWT_SECRET=
# JWT_ALGORITHM=HS256
# JWT_ACCESS_TOKEN_TTL_MINUTES=60
# JWT_REFRESH_TOKEN_TTL_DAYS=14

# Encryption (calendar tokens, failure memos)
# COLUMN_ENCRYPTION_KEY=
